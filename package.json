{
  "name": "second-example",
  "version": "1.0.0",
  "description": "Jimmy Lima Campaign Site — Second Example",
  "scripts": {
    "build": "esbuild entry.tsx --bundle --outfile=bundle.js --loader:.tsx=tsx --loader:.ts=ts --loader:.webp=file --external:three",
    "watch": "esbuild entry.tsx --bundle --outfile=bundle.js --loader:.tsx=tsx --loader:.ts=ts --loader:.webp=file --external:three --watch"
  },
  "dependencies": {
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
